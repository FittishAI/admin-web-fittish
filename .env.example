# Copy this file to .env.local for local development.
#
# Local development should point at staging, never production.
# On Netlify this value comes from the per-deploy-context environment variable,
# so it is NOT read from any committed .env file.
NEXT_PUBLIC_API_URL=https://staging-api.fittish.ai
